arm_tenant_id           = "${ARM_TENANT_ID}"
arm_subscription_id     = "${ARM_SUBSCRIPTION_ID}"
arm_client_id           = "${ARM_CLIENT_ID}"
arm_client_secret       = "${ARM_CLIENT_SECRET}"

ghost_version           = "${GHOST_VERSION}"

app_name                = "${APP_NAME}"
organization_abv        = "${ORGANIZATION_ABV}"
primary_location        = "${PRIMARY_LOCATION}"
primary_location_abv    = "${PRIMARY_LOCATION_ABV}"
secondary_location      = "${SECONDARY_LOCATION}"
secondary_location_abv  = "${SECONDARY_LOCATION_ABV}"

cicd_resource_group     = "${CICD_RESOURCE_GROUP}"
cicd_container_registry = "${CICD_CONTAINER_REGISTRY}"
